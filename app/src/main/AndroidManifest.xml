<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.windows10.newproject">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".RegistrationMember"
            android:label="Register Member" />
        <activity
            android:name=".favourite_food_list"
            android:label="Favourite Food List" />
        <activity
            android:name=".RegistrationRider"
            android:label="Rider Registration" />
        <activity
            android:name=".RegistrationResOwner"
            android:label="Register Restaurant Owner" />
        <activity
            android:name=".FoodList"
            android:label="Food List" />
        <activity
            android:name=".FoodDetail"
            android:label="Food Detail" />
        <activity
            android:name=".Cart"
            android:label="Cart" />
        <activity
            android:name=".FAQs"
            android:label="FAQs" />
        <activity
            android:name=".ReportDeliverIssues"
            android:label="Report Delivery Issues" />
        <activity
            android:name=".about"
            android:label="About" />
        <activity
            android:name=".Help1"
            android:label="Help" />
        <activity android:name=".LoginRes" />
        <activity android:name=".LoginRider"></activity>
    </application>

</manifest>